{
  "name": "backend",
  "version": "1.0.0",
  "description": "server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd project-app && npm run build",
    "install-project-app": " cd project-app && npm install",
    "start": "nodemon server.js",
    "client": "cd project-app && npm start",
    "heroku-postbuild": "npm run install-project-app  && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.3",
    "helmet": "^3.23.3",
    "nodemon": "^2.0.4"
  },
  "engines": {
    "node": "12.18.0",
    "npm": "6.14.4"
  }
}
